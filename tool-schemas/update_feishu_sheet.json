{
  "name": "update_feishu_sheet",
  "description": "Updates properties of a worksheet (e.g. title).",
  "arguments": {
    "type": "object",
    "properties": {
      "spreadsheetToken": {
        "type": "string",
        "description": "Feishu spreadsheet token (required). Found in the URL: https://xxx.feishu.cn/sheets/{spreadsheetToken}. Typically starts with \"shtc\"."
      },
      "sheetId": {
        "type": "string",
        "description": "Sheet ID (required)."
      },
      "title": {
        "type": "string",
        "minLength": 1,
        "maxLength": 100,
        "description": "New title of the sheet (required)."
      }
    },
    "required": ["spreadsheetToken", "sheetId", "title"],
    "additionalProperties": false,
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
